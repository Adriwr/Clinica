{% extends 'base.html.twig' %}

{% block css %}

{%  endblock %}

{% block dashboard %}
    <h3 class="page-header">Consulta de articulos vendidos en {{ mensaje }}</h3>
{% endblock %}

{% block content %}
    {% if articulos %}
    {% for articulo in articulos %}
        <tr>
        {% if ("now"|date('Y'))|number_format == (articulo.fecha|date('Y'))|number_format %}
            {% if ("now"|date('m'))|number_format == (articulo.fecha|date('m'))|number_format %}
                {% if ("now"|date('d'))|number_format == (articulo.fecha|date('d'))|number_format %}
                        <td>
                            {{ articulo.nombre }}
                        </td>
                        <td>
                            {{ articulo.numero }}
                        </td>
                {% endif %}
            {% endif %}
        {% endif %}

        </tr>

    {% endfor %}
        {% else %}
            <div class="warning">
                <p class='text-danger'> No hay ventas para mostrar </p>
            </div>
    {% endif %}
{% endblock %}

{% block javascript %}
    {# Plugins requeridos por el modulo #}

{% endblock %}
