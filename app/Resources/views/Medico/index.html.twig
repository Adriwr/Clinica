{% extends 'base.html.twig' %}

{% block dashboardHeader %}
    Areas
{% endblock %}

{% block content %}
    <div class="row"∫>
        <div class="col-xs-12 drawer-height" id="full">
            <div class="row drawer-height drawer-relative">
                <div id="drawer-capture"  class="col-xs-6 col-sm-offset-3 drawer-content">
                    <div class="col-xs-12">
                        {{ form_start(form , {'attr': {'class': 'form-horizontal'} }) }}
                        {{ form_widget(form ) }}
                        {{ form_end(form) }}
                    </div>
                </div>
            </div>
        </div>
    </div>
{% endblock %}


